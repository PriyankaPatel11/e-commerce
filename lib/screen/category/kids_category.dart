import 'package:flutter/material.dart';

class KidsCateegory extends StatelessWidget {
  const KidsCateegory({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: EdgeInsets.all(2),
      child: ClipRRect(
        child: Container(
          child: Column(
            children: [
              
            ],
          ),
        )),
      );
  }
}